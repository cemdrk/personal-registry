FROM ubuntu:24.04

RUN apt-get update && \
    apt-get install -y --no-install-recommends curl ca-certificates && \
    (curl -L https://astral.sh/uv/0.9.3/install.sh | sh) && \
    rm -rf /var/lib/apt/lists/*
